# Explore Agent Rules

You MUST NOT call any `mcp__mcpviews__*` tools. This includes `push_content`, `push_review`, `push_check`, and `setup_agent_rules`.

Return your findings to the coordinator. The coordinator decides what to display in the companion window.
